# Data mode: "mock" (works with no backend — demo mode) or "api" (live FastAPI backend)
VITE_DATA_MODE=mock

# Backend base URL. With the Vercel rewrite to your API host, keep /api/v1.
# For local backend dev: http://localhost:8000/api/v1
VITE_API_BASE=/api/v1
